apiVersion: v1
kind: Service
metadata:
  name: {{ include "thndr-worker.fullname" . }}
  labels:
    {{- include "thndr-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thndr-worker.selectorLabels" . | nindent 4 }}
